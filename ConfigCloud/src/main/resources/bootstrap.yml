server:
    port: 8086

spring:
  cloud:
    config:
      server:
        git:
            uri:https://github.com/renierba/ProjectCustomer.git
            searchpath:ConfigData
            username:renierba
            password:Rbn1124#
    security:
        user:
            name:root
            password:secret
        